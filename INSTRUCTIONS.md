# Инструкция по запуску проекта Student Portal

## Требования

- Docker Desktop (включая Docker Compose)
- 4GB+ свободной RAM
- Порты 3000, 8080, 5432 должны быть свободны

## Быстрый старт

### Windows (PowerShell)

```powershell
.\start.ps1
```

### Linux/Mac

```bash
chmod +x start.sh
./start.sh
```

### Или вручную

```bash
docker-compose up --build
```

## После запуска

Приложение будет доступно через несколько минут:

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8080
- **PostgreSQL**: localhost:5432

## Тестовые аккаунты

### Администратор
- Email: `admin@example.com`
- Пароль: `admin123`
- **Права**: Полный доступ ко всем данным, управление пользователями и всеми таблицами БД

### Преподаватель
- Email: `teacher@example.com`
- Пароль: `teacher123`
- **Права**: Выставление оценок, управление лабораторными, посещаемостью и аттестациями

### Студент
- Email: `student@example.com`
- Пароль: `student123`
- **Права**: Просмотр своих оценок, лабораторных, посещаемости и аттестаций

**💡 Совет**: На странице входа есть кнопки "📋 Копировать" для быстрого заполнения полей входа!

## Остановка приложения

Нажмите `Ctrl+C` в терминале, затем:

```bash
docker-compose down
```

Для полной очистки (включая базу данных):

```bash
docker-compose down -v
```

## Структура проекта

```
.
├── backend/                 # Java Spring Boot бэкенд
│   ├── src/
│   │   └── main/
│   │       ├── java/
│   │       │   └── com/studentportal/
│   │       │       ├── controller/    # REST контроллеры
│   │       │       ├── model/         # JPA модели
│   │       │       ├── repository/    # Spring Data репозитории
│   │       │       ├── security/      # JWT конфигурация
│   │       │       ├── service/       # Бизнес-логика
│   │       │       └── dto/           # Data Transfer Objects
│   │       └── resources/
│   │           └── application.yml    # Конфигурация приложения
│   ├── Dockerfile
│   └── pom.xml
│
├── frontend/                # React фронтенд
│   ├── public/
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── services/        # API сервисы
│   │   ├── App.js
│   │   └── index.js
│   ├── Dockerfile
│   ├── nginx.conf
│   └── package.json
│
└── docker-compose.yml       # Docker Compose конфигурация
```

## Функционал

### Для администратора:
- ✅ Полное управление пользователями (создание, редактирование, удаление)
- ✅ Создание студентов, преподавателей и других администраторов
- ✅ Управление предметами
- ✅ Просмотр и редактирование всех данных в БД:
  - Оценки
  - Лабораторные работы
  - Посещаемость
  - Аттестации
- ✅ Удаление любых записей из всех таблиц
- ✅ Табличный интерфейс с фильтрацией

### Для преподавателя:
- Выставление оценок студентам
- Начисление баллов за лабораторные работы
- Отметка посещаемости
- Проставление аттестаций (первая, вторая, финальная)
- Управление предметами
- Просмотр списка всех студентов

### Для студента:
- Просмотр своих оценок
- Список лабораторных работ с баллами
- Таблица посещаемости
- Информация об аттестациях
- Список преподавателей
- Dashboard с общей статистикой

### Дополнительно:
- 📋 Кнопки копирования тестовых данных на странице входа
- 🎨 Современный градиентный UI
- 📱 Адаптивный дизайн
- ⚡ Быстрая навигация между разделами

## API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход
- `POST /api/auth/register` - Регистрация

### Студент (требуется авторизация)
- `GET /api/student/{id}/grades` - Получить оценки
- `GET /api/student/{id}/labs` - Получить лабораторные
- `GET /api/student/{id}/attendance` - Получить посещаемость
- `GET /api/student/{id}/attestations` - Получить аттестации
- `GET /api/student/{id}/dashboard` - Получить статистику
- `GET /api/student/teachers` - Получить список преподавателей

### Преподаватель (требуется роль TEACHER)
- `GET /api/teacher/students` - Список студентов
- `POST /api/teacher/grades` - Добавить оценку
- `POST /api/teacher/labs` - Добавить лабораторную
- `POST /api/teacher/attendance` - Отметить посещение
- `POST /api/teacher/attestations` - Добавить аттестацию
- `POST /api/teacher/subjects` - Добавить предмет

## Технологии

### Backend
- Java 17
- Spring Boot 3.2
- Spring Security + JWT
- Spring Data JPA
- PostgreSQL 15
- Lombok
- Maven

### Frontend
- React 18
- React Router 6
- Axios
- CSS3 (современный градиентный дизайн)

### Infrastructure
- Docker
- Docker Compose
- Nginx (для production frontend)

## Troubleshooting

### Порты заняты
Измените порты в `docker-compose.yml`:
```yaml
ports:
  - "YOUR_PORT:3000"  # Frontend
  - "YOUR_PORT:8080"  # Backend
  - "YOUR_PORT:5432"  # PostgreSQL
```

### База данных не создается
```bash
docker-compose down -v
docker-compose up --build
```

### Backend не может подключиться к БД
Подождите 30-60 секунд - PostgreSQL требует время для инициализации.

### Frontend не может подключиться к Backend
Проверьте, что Backend запустился успешно:
```bash
docker logs student_portal_backend
```

## Разработка

### Backend
```bash
cd backend
mvn spring-boot:run
```

### Frontend
```bash
cd frontend
npm install
npm start
```

## Production

Для production сборки уже настроены Dockerfile с multi-stage build для оптимизации размера образов.

Запуск в production:
```bash
docker-compose -f docker-compose.yml up -d
```

---

**Создано для курсового проекта** 🎓


